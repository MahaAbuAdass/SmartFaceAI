<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow access to files in the app's files directory -->
    <files-path name="files" path="." />
    <!-- Allow access to files in the app's cache directory -->
    <cache-path name="cache" path="." />
    <!-- Allow access to files in the app's external cache directory -->
    <external-path name="external_cache" path="Android/data/com.example.detectionpython/cache/" />
</paths>
